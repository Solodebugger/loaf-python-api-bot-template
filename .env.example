# Copy to .env and fill in. The SDK reads these when arguments are omitted.

# Your Loaf USER API KEY — used as the Bearer token.
# Mint one from the Loaf web app: log in -> Settings -> API keys -> Create.
# (API keys can only be created/listed/revoked while logged in to the web app;
#  a bot then authenticates with the key string it produced.)
LOAF_API_KEY=

# REST base URL, INCLUDING the trailing /api.
#   Production:     https://api.loafmarkets.com/api
LOAF_API_BASE_URL=https://api.loafmarkets.com/api

# Optional: override the WebSocket URL. If unset it is derived from the base URL
# (https://host/api -> wss://host/ws, http://host/api -> ws://host/ws).
# LOAF_WS_URL=ws://localhost:8005/ws

# Optional: your numeric Loaf user id (from the web app). Only needed to
# subscribe to the PRIVATE portfolio WebSocket channel (bot.py / example 05).
# LOAF_USER_ID=
